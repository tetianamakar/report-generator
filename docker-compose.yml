version: '3.9'
services:
  postgres:
    image: postgres
    ports:
      - "5438:5432"
    environment:
      - POSTGRES_DB=companies
      - POSTGRES_USER=tetiana
      - POSTGRES_PASSWORD=password
